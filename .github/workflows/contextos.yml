name: Contexto
run-name: ¡Estoy aprendiendo a usar Contextos!
on: [push]

jobs:
  info-basica:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar info del repo
        run: |
          echo "en el Repositorio: ${{ github.repository }}"
          echo "en la Rama: ${{ github.ref_name }}"
          echo "en el Evento: ${{ github.event_name }}"
          echo "Actor fue: ${{ github.actor }}"
  not-main:
    if: ${{ github.ref != 'refs/heads/main' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Esta no es la rama main, es ${{ github.ref_name }}"
